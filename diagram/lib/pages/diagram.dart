import 'package:flutter/material.dart';

class DiagramPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Diagram test'),
      ),
      body: Center(
        child: Text('Test on'),
      ),
    );
  }
}
